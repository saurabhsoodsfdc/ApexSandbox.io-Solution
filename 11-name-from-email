public String nameFromEmail(String email) {
    String firstName = email.substringBefore('.');
    String lastName = emai.substringBetween('.', '@');
    return capitalize(firstName) + ' '+ capitalize(lastName);
}